let numeros = [1, 2, 3];
let numeros2 = numeros.map(n => n * 2);

console.log(numeros2);

// A função map() é utilizada para mapear todos os elementos de um array para outro array
// Este códigp vai multiplicar cada item do array por 2