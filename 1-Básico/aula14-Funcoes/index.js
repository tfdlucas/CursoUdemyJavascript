/*

//FUNÇÃO PADRÃO 

function soma(x, y) { // nome da funçao, dentro de parenteses é o parametro
    const resultado = x + y; 
    return resultado; // Este é o valor que será retornado na variavel
}

console.log(soma(2, 2));  //Chamando a função | Os dois valores substituirao os parametros


//FUNÇÃO ANONIMA

const raiz = function (n) {
    return n ** 0.5;
}; // Quando declarada em variaveis precisa ter o ponto e virgula

console.log(raiz(9));


//ARROW FUNCTION

const raiz = n => n ** 0.5; // Precisa de parenteses no parametro quando for mais de um argumento.

console.log(raiz(25));
*/